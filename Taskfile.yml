# https://taskfile.dev

version: '3'

dotenv:
  - ./.env

includes:
  docs: 
    dir: ./docs
    taskfile: ./docs/Taskfile.yml
  scripts: ./scripts
  #dataset: ./dataset

tasks:
  default:
    cmds:
      - npm start
    silent: true
